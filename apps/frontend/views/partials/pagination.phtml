<?php if (isset($page)): ?>
    <nav>
        <ul class="pagination">
            <li>
                <a href="<?= $page->beforeQuery; ?>">Previous</a>
            </li>
            <li>
                <a href="<?= $page->nextQuery; ?>">Next</a>
            </li>
        </ul>
    </nav>
    <?php echo "You are in page ", $page->current, " of ", $page->total_pages; ?>
<?php endif; ?>
